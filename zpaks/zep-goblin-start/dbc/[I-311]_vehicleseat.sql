-- I-311 "Warchief's Revenge" (quest 14243) — Cyclone of the Elements seat 5746
-- (vehicle 508, creature 36178). The raw Cata flags 0x60110284 read under WotLK
-- semantics lack VEHICLE_SEAT_FLAG_CAN_ENTER_OR_EXIT 0x2000000 — the server
-- refuses the voluntary exit, trapping the rider (Cata-bit disease, I-248/I-308
-- family) — and flags_b 0x82C00 is Cata noise.
-- Donor: stock WotLK Stormcrest Eagle rider seat 2112 (vehicle 196, Brunnhildar
-- quests) — the same genre exactly: auto-piloted flying vehicle, rider casts bar
-- spells (CAN_CAST 0x20000000), voluntary exit allowed, no CAN_CONTROL 0x800
-- (SmartAI flies the circuit/ferry, zz_[I-311]_warchiefs_revenge_cyclone.sql).
-- The bar on an uncontrolled seat needs the I-311 core patch (retail serves it;
-- stock AC only sends it via the CAN_CONTROL charm path).
-- Flags wholesale from the donor, Cata anim/attachment/camera columns kept.
-- AUTO-owned row -> one consolidated UPDATE.
-- NOTE: flags are read by BOTH sides — client via PATCH-Z, server via
-- data/dbc/VehicleSeat.dbc (regenerated by the PATCH-Z patch-mpq build, NOT by
-- `zep dbc bin export`) — so: dbc apply -> PATCH-Z build -> worldserver restart.
UPDATE vehicleseat SET
  `flags` = 1660977199,  -- 0x6300802F
  `flags_b` = 0
WHERE id = 5746;
