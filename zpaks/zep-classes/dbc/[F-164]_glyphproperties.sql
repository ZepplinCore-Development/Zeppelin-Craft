-- [F-164] Earthwarden glyphs - glyphproperties.dbc legacy cleanup.
-- glyphproperties is a newly-managed DBC table (imported into original/live/expected
-- via DBCTool with meta/glyphproperties.meta.json; 3.3.5 layout = id, spell_id,
-- glyph_slot_flags, spell_icon_id). Stock IDs run 1-911.
--
-- The actual Earthwarden glyph rows (901-907) have all been migrated to their
-- per-glyph sub-feature files (single-file ownership), each of which DELETE+INSERTs
-- its own id:
--   901/902/906 -> [F-164R]_glyphproperties.sql   (Rockslam/Rocksurge/Rockwall)
--   904         -> [F-164B]_glyphproperties.sql   (Crag Strike)
--   905         -> [F-164D]_glyphproperties.sql   (Volcanic Shield)
--   903         -> [F-164A]_glyphproperties.sql   (Tectonic Blast)
--   907         -> [F-164T]_glyphproperties.sql   (Thunderborne Leap)
--
-- F-164 PARENT retains ONLY this legacy purge of the earlier out-of-range glyph IDs
-- (90001-90007 = uint16 fail, 6001-6007 = auto-inscribe fail) that predate the final
-- 901-907 stock-range assignment. It is general hygiene, not any single glyph's data,
-- so it stays parent-owned. Must NOT also delete 903/907 here — the sub-feat files
-- own those rows and a parent DELETE could wipe their freshly-inserted row on apply.
DELETE FROM `glyphproperties` WHERE `id` IN (90001, 90002, 90003, 90004, 90005, 90006, 90007, 6001, 6002, 6003, 6004, 6005, 6006, 6007);
